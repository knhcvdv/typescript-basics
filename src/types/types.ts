// ============================================
// Типи для інтерактивного додатку
// ============================================

// Тип для поста з API
export type Post = {
    userId: number;
    id: number;
    title: string;
    body: string;
};